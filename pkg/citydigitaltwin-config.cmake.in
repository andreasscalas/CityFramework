# Compute installation prefix relative to this file.
get_filename_component(_dir "${CMAKE_CURRENT_LIST_FILE}" PATH)
get_filename_component(_prefix "${_dir}/../.." ABSOLUTE)

# Import the targets.
include("${_prefix}/lib/CityDigitalTwin-@version@/CityDigitalTwin-targets.cmake")

# Report other information.
set(CityDigitalTwin_INCLUDE_DIRS "${_prefix}/include/CityDigitalTwin-@version@/")

file(GLOB CityDigitalTwin_LIBRARIES "${_prefix}/lib/CityDigitalTwin-@version@/lib*")
